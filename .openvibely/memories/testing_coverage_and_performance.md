---
name: testing_coverage_and_performance
type: project
created: 2026-06-07
updated: 2026-08-31
source: after_complete_update
source_id: 578cd3e66286902ba2b381bacaef8b20:f2f3ed9e55f6df66
confidence: high
title: Testing Coverage and Performance
---

OpenVibely has a large Go test suite. Durable quality work should emphasize broad behavioral coverage and production-shaped performance evidence rather than narrow happy paths, raw logs, or task-by-task benchmark notes.

Coverage and fixture contracts:
- `internal/service` and `internal/handler` are the broadest coverage seams, especially error, pagination, webhook, retry, scheduling, channel, and orchestration paths. `internal/service/llm_service.go` needs controlled LLM caller mocks to avoid flaky tests.
- Generated `*_templ.go` files are excluded from coverage summaries while template tests still run. Prefer behavioral coverage over granular tests around already-covered code, and avoid blanket `t.Parallel()` around shared database setup.
- `NewTestDB` uses isolated in-memory fixtures from an immutable serialized SQLite template with UTC, foreign keys, busy timeout, one connection, migrated schema, seed data, cleanup, and default-agent behavior.
- Runtime-tool/action tests cover ownership, authorization, pagination, mode restrictions, malformed JSON, provider-tool-incapable paths, and case-insensitive JSON field presence. Explicit numeric zero values must be tested as supplied invalid input, not mistaken for omission.
- Resolved issue `#941` in PR `#948`: X mention-ingress regressions drive `pollOnce` through the real mention handoff and assert prompt/message content for exact-case, mixed-case, prefix, repeated, punctuation-adjacent, ordinary at-sign, and empty-username inputs. The same production path retains one-task, durable-receipt/redelivery, and cursor assertions while proving only the authenticated full-handle mention is removed case-insensitively. A fresh strictly read-only audit of published head `7112bf795faa8e2f9eef5020d3bd5c2cfdf3a1f3` found no material bugs, regressions, missing requirements, or scope violations; no build or tests were rerun during that audit.
- Browser task-goal tests cover explicit/selected project ownership for get/set/pause/resume/clear; missing scope, unknown tasks, and foreign tasks must fail without goal reads, leakage, or mutation.
- Changes-tab merge coverage uses the production handler and browser path to prove a recoverable failed merge returns authoritative retry actions, a second submission can reach the merged terminal state, all alternate action clicks are suppressed while one request is active, and genuine conflicts render guidance plus Resolve/Abort while suppressing ordinary merge actions. Repair commit `2009a0f7` added the menu-wide browser guard, a canonical repository-path lease, live render/merge-POST eligibility checks, stale persisted-conflict reconciliation, and failed Resolve/Abort refresh coverage. The 2026-08-30 follow-up repair added lease-held live task/ancestry revalidation for merge requests, the same complete eligibility and actual divergence gates for forged/stale Rebase requests, automatic cleanup of squash conflicts without `MERGE_HEAD` to retryable failure, and legacy task-scoped Abort recovery for such conflicts while preserving unrelated staged work. A subsequent implementation repair extended the lease to Rebase, made its key process-wide and canonical across absolute paths, symlinks, linked worktrees, and separate service instances via the Git common directory, and changed staged/conflict discovery to exact NUL-delimited parsing for whitespace/newline filenames. Focused race/concurrency, alias/linked-worktree, unusual-path, conflict, and squash regressions passed, along with `go build ./cmd/server`, serialized `go test -p 1 ./internal/... -count=1 -timeout 120s`, `go test ./web/templates/pages -count=1 -timeout 120s`, and `git diff --check` on 2026-08-30. A fresh strictly read-only audit at exact head `43d621c47ec1bb65c98ae88b90d9c66250774ccc` found one high material gap: `ResolveTaskConflicts` and `AbortTaskMerge` call conflict-recovery mutations without the shared repository lease, so concurrent resolve/abort or a new merge/rebase can race and leave unexpected Git/task state; current menu-wide disabling and sequential browser coverage do not prove safety against forged/concurrent requests. The prior merge/rebase lease canonicalization and exact-path issues are resolved. The audit made no workspace changes or validation runs, confirmed a clean worktree five commits ahead and zero behind `main`, and another fresh audit is required after the conflict-recovery lease fix. The follow-up implementation repair in commit `9335351d` now keeps `HandlePostExecution` conflict Resolve and fallback Abort on validated, canonical Git-common-directory lease paths, revalidates refreshed task branch/target and live `MERGE_HEAD` ownership/unmerged files, and performs handler Abort Git cleanup plus `merge_status=pending` persistence before releasing the lease; auto-recovery fallback persists `conflict` in the same lease. Focused real-Git/service race and handler regressions cover foreign conflicts, lease-atomic status, concurrent recovery, and authoritative Changes refresh. `go build ./cmd/server`, `go test -p 1 ./internal/... -count=1 -timeout 180s`, `go test ./web/templates/pages -count=1 -timeout 180s`, and `git diff --check` passed; the implementation worktree is clean. A fresh strictly read-only audit at exact head `d2d13a2d38a5fa61884c60ccc84d645d9e9a0f00` found no material bugs, regressions, missing requirements, or scope violations, confirmed generated output matched the reviewed sources, and made no workspace changes or validation runs.
- Modifier-selection browser regressions cover both Tasks and Schedules: production-rendered command/Ctrl-click must still toggle selection without a global counter/Clear overlay, and schedule selection must preserve card dimensions while grouped drag, rollback, and authoritative-refresh behavior remains intact. The schedule regression also covers the `overflow-hidden` card edge case: the selected inset overlay must cover the existing left accent border without painting a doubled inner left stripe; `TestScheduleCardsSupportModifierMultiSelectGroupedPointerDragAndRollback` passed in Chromium after the focused CSS fix.
- Tasks Kanban production-shaped Chromium coverage verifies two-card Command/Ctrl multi-selection survives an authoritative queued-to-running refresh, focused task-card and column/dropzone menu options retain exact focus through replacement when they survive, removed options fall back to the invoker, and trigger, Escape, outside-activity, navigation, and invoker-removal dismissal leave no stale menu state that can reopen on a later refresh. The unchanged drag/drop path remains covered in the same production-rendered flow. On 2026-08-30, coverage was expanded with navigation/history restoration that clears document-global selection and menu state, grouped category and Active-lane drops that clear selection before HTMX request snapshotting while preserving copied task IDs, and native keyboard-operable column controls. The regressions exercise both grouped mutation branches and stale internal selection synchronization alongside existing refresh/focus/dismissal/drag behavior. A fresh strict read-only audit of the completed implementation found no material bugs, regressions, or missing requirements, confirmed generated output matched the reviewed sources and the worktree was clean, and made no workspace changes or validation runs.
- Task-card merge-menu coverage uses production-rendered Chromium and backend handler tests for lazy authoritative refreshes on every open and board replacement, eligible/ineligible and stale-state options, project ownership, dirty/locked/task-status/conflict gates, PR state and confirmed `Create PR`, merge/rebase/PR failure and retry behavior, duplicate-submit protection, authoritative board replacement, selection/focus retention and fallback, dismissal/navigation cleanup, and narrow/bottom-edge menu geometry. The repair after the strict audit at exact head `91028aa560180cde05082a65838313fe3711faec` adds regressions for linked-worktree versus main-checkout lease identity, initial finalization waiting on the shared mutation boundary, end-to-end task status remaining non-terminal until finalization completes, closed historical PR rendering, endpoint-level stale-state checks, and the generalized Kanban menu lifecycle. Upstream's newer process-wide canonical Git-common-directory lease and conflict-recovery behavior were retained and composed with blocking setup/startup-sync/task-output/cleanup/orphan-cleanup/PR-publication writers. Final head `ab4838e6f5fde7ff38875ead6737b7ab3a0978a6` passed zero-update `make templ`, `go build ./cmd/server`, all affected service/handler/component/layout/page suites including production Chromium coverage, focused `-race` tests, uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check`; only expected macOS linker warnings appeared. The worktree is clean, six commits ahead and zero behind current `main`, and a separate fresh strictly read-only audit is still required. That audit at exact head `ab4838e6f5fde7ff38875ead6737b7ab3a0978a6` found a material projection gap: card-option coverage does not exercise blocked/pending tasks or missing/stale task and target refs, and the card endpoint can advertise merge controls for those states because it does not share the canonical terminal-status/ref eligibility checks. The mutation POST still revalidates safely under the repository lease. The separate implementation repair in commit `6e0dfbd8f48f9a82b4e46110067f9ca7b93aa0c1` now routes `taskCardMergeEligibility` through the canonical task-detail/Changes eligibility resolver. Endpoint regressions cover pending and blocked tasks plus missing task and target refs, proving local merge controls are suppressed while independent `Create PR` eligibility remains available; the existing conflict coverage now creates a real owned Git conflict. `make templ` reported zero updates, `go build ./cmd/server`, focused and full handler tests, uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check` passed. The worktree is clean, `0 behind / 7 ahead` of `main`, and a fresh separate strictly read-only audit is required after this fix. That audit at exact head `6e0dfbd8f48f9a82b4e46110067f9ca7b93aa0c1` found one medium material card-Rebase response bug: if live eligibility changes while a card-originated Rebase waits for the repository lease, `RebaseTaskBranch` returns a 200 `GetTaskChanges` fragment to an HTMX request targeting `#kanban-board`; the board can therefore be replaced by task Changes markup and the modal treated as successfully completed. Coverage exercises pre-lease Rebase rejection and lease-time Merge revalidation, but not this lease-time card-Rebase path. The separate non-audit repair committed as `892d6ea972f2a94ddd483a6750971d69d24e42de` now routes card-originated `ErrMergeEligibilityChanged` Rebase responses through the card mutation rejection path, returning HTTP `409` with the failure toast instead of a `200` Changes fragment while preserving authoritative Changes refreshes for non-card callers. `TestHandler_TaskCardRebaseRevalidatesEligibilityAfterRepositoryLease` holds the repository lease, changes the task to blocked before release, and proves the `409`, blocked reason/toast, and absence of `changes-actions-dropdown` markup. `make templ` reported zero updates; the server build, focused/full handler tests, focused race test, full uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check` passed. An unrelated first-run Analytics Chrome profile-cleanup race passed in isolation and on one fresh full-suite rerun. The exact clean head is `892d6ea972f2a94ddd483a6750971d69d24e42de`, with `main` an ancestor, `0 behind / 8 ahead`, and no upstream or remote ref containing the head; a fresh separate strictly read-only audit is still required.
- Issue `#939` is covered by descriptor-level and rendered card/Worktree/Changes assertions: canonical local merge labels, types, endpoints, target text, card metadata consumption, conditional Rebase, `merge_source`/project context, HTMX targets, and existing state visibility remain locked while pull-request and conflict-recovery paths stay separate. Focused component/page and handler/browser suites passed, along with `make templ`, `go build ./cmd/server`, uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check`; only existing macOS linker-version warnings appeared for the desktop test binary. The implementation is in PR `#949` from commit `3e7797db`.
- The missing-task card Create PR regression in `internal/handler/worktree_pull_request_test.go` (`TestCreateTaskPullRequest_MissingTaskCardRequestReturnsNonSuccess`) reproduces the old successful-looking `204`, then verifies HTTP `400`, failure-toast metadata, and no board/Changes replacement fragment after card-source detection was moved before task lookup in commit `801c2b2ce23a2372723d45e3a545d176ea9d8667`. Focused and full handler tests, `go build ./cmd/server`, zero-update `make templ`, uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check` passed; only expected macOS linker warnings appeared. A fresh strictly read-only audit at exact head `801c2b2ce23a2372723d45e3a545d176ea9d8667` found one medium material ownership-oracle bug in that repair: missing task-card PRs return HTTP `400` while existing foreign-project tasks return `404`, exposing whether a task ID exists. Card ownership failures should remain indistinguishable while retaining a non-2xx failure/toast and no replacement fragment; the audit made no workspace changes or validation runs, so another repair and fresh audit are required. The follow-up repair in commit `26e5f027fff716dc03bc54075098298a9ac49d14` now routes missing and foreign-project card task IDs through the same HTTP `404` `Task not found` response with identical failure-toast metadata and no replacement fragment, while preserving non-card missing-task behavior. `TestCreateTaskPullRequest_TaskCardOwnershipFailuresAreIndistinguishable` compares status, body, toast metadata, and fragment absence. `make templ` reported zero updates; focused and full handler tests, `go build ./cmd/server`, uncached `go test ./... -count=1 -timeout 300s`, and `git diff --check` passed, with only expected macOS linker warnings. The exact clean head is `26e5f027fff716dc03bc54075098298a9ac49d14`, `0 behind / 10 ahead` of `main`. A fresh separate strictly read-only audit at that exact head found no material bugs, regressions, missing requirements, or scope violations, confirmed generated output matched the reviewed sources, made no workspace changes or validation runs, and the task goal is complete.
- Alert inspection coverage should exercise compact list rendering, project-scoped HTMX detail hydration, shared Markdown/code/link sanitization, parser/configuration fallback, exact raw-body copying including LF, CRLF, and bare-CR preservation, empty-body behavior, and computed light-mode inline-code contrast through service/template and real-browser regressions. `TestAlertsInspectMarkdownAndHTMXDetailLoadingInChrome` verifies at least 4.5:1 contrast after hydration while preserving fenced-code behavior.
- A production-shaped integration regression must invoke the actual service/repository path and shared SSE/router boundary. Synthetic fixture mutations or manually dispatched events do not prove claim, broadcaster, transport, listener, project isolation, or authoritative refresh behavior. New coverage must preserve existing ordinary regressions rather than replacing them.
- Task-card state-icon coverage verifies the compact repository projection for achieved goals, the standard award-ribbon/check `Goal met` icon only for completed tasks, merged-over-goal precedence, stale goal metadata on non-completed states, and the live HTMX board morph path. The canonical `Goal met` award-ribbon/check SVG is defined in `docs/task-status-icon-options.html`; `TestTaskCard_GoalMetIconMatchesDocumentedStandardGlyph` locks the documented state and exact geometry to the rendered card markup.
- Run-now Automation Live freshness has production-shaped backend and Chromium coverage: after seeding a previously failed scheduled task, the actual manual-dispatch path renders the exact new claimed/dispatched/running invocation before model output, rejects delayed stale failed fragments across authoritative refresh and hard reload, preserves project/task isolation, and returns to red only for the new invocation's terminal failure. On 2026-08-30, reconnect coverage was made production-shaped in `internal/handler/automation_capacity_live_browser_test.go`: the test renders the full `pages.AutomationLive` document, uses the real `Handler.LiveEventsSSE` route and shared sidebar coordinator, cancels the first server-side stream, requires a second native EventSource connection, and verifies the real reconnect `onopen` dispatch refreshes a stale failed snapshot to running through the existing project/Automation-scoped generation guards. Existing Run now, delayed-response, refresh, reload, isolation, and terminal-failure checks remain. No application implementation change was needed; focused Chromium coverage, `go build ./...`, `go vet ./...`, uncached `go test ./internal/... ./web/... -count=1 -timeout 240s`, and `git diff --check` passed. A separate fresh strict read-only audit found no material bugs, regressions, or missing requirements and made no workspace changes.
- On 2026-08-30, the primary-action follow-up fixed the imported-theme parity gap by deriving `--ov-primary-action-color` from the selected non-native theme's DaisyUI `--p` channel and making both `.chat-send-button` and `.task-state-running` consume it without `!important`. Rendered CSS assertions and `TestTaskRunningIconSharesThemeAwareSendColorWithoutOverridingImportedThemeInChrome` cover native dark/light normal equality, imported normal equality and distinction from native, preservation of imported primary styling, and the absence of shared Send overrides that suppress hover. The subsequent coverage repair drives Chrome's actual pointer through CDP and verifies computed normal and `:hover` colors for native dark, native light, and imported themes; it confirms the Send color changes on hover while the running icon retains the normal shared token. That interaction exposed a cascade issue in which native generic/primary rules matched imported themes; native generic/primary hover rules and the native light primary base rule are now scoped to their native `data-color-theme` IDs, preserving imported primary/hover behavior without `!important` on shared Send rules. Deterministic templ generation, the server build, all internal tests, all template tests, and `git diff --check` passed. A fresh strict read-only audit of exact head `279f16dc40a1468d0904a0e2d6026013b59a5471` found no material bugs, regressions, or missing requirements, confirmed the worktree clean and the branch `0 behind / 5 ahead` of `main` with no task-side merge commits, and made no workspace changes or validation runs. The same task branch was later rebased onto current `main` at `9c5e22f5` as `29ef7159`; generated-only conflicts in `base_templ.go` were resolved by regenerating from the clean `.templ` source, and the result remained clean at `0 behind / 5 ahead`. The server build, deterministic generation, focused layout/component tests, and Tasks Chromium regressions passed. The full layout package still fails `TestBaseProvidesCentralClientSidePageTitleAndHistorySynchronization` because its Node fixture calls undefined `resetKanbanInteractionForNavigation`; the failure reproduces on `main` and is a baseline fixture issue, not a rebase regression. No merge to `main` was performed.
- On 2026-08-30, card-pagination commit `8153885e44828c9b312afde0fcf2570bdd33224a` added `TestCardPaginationProductionBrowserRestoresFocusFromFixedPersonalityCard`: the regression fails before the fix when deleting the focused selected custom Personality card, then passes when replacement focus lands on a visible surviving card without reopening a dropdown. Follow-up commit `8608eaab8f61c8f6598e5634a18380a55bfc7dfd` added `TestCardPaginationProductionBrowserRestoresFocusFromFixedChannelCard` and Channels root-wide replacement scope: a production-rendered focused GitHub `Disconnect` action survives the actual Channels root replacement with visible focus restored, no dropdown reopening, and webhook pagination state unchanged. The shared replacement selector may include fixed cards for snapshots/lookup, but page-owned pagination remains list-scoped. Commit `413ac15358effff2987f24f96b26e4e85d867e98` keeps pagination completion silent; `TestCardPaginationCompletionIsSilent` verifies the rendered status retains loading/error/retry/sentinel hooks without an “End of list” marker, while browser regressions observe the root pagination state’s authoritative `hasMore=false`. Deterministic `make templ` (zero updates after the change), server build, focused pagination/browser coverage, `go test ./internal/...`, `go test ./web/templates/pages`, `go test ./...`, and `git diff --check` passed; the task worktree was clean. A separate fresh strict read-only audit of exact head `413ac15358effff2987f24f96b26e4e85d867e98` then found no material bugs, regressions, or missing requirements, confirmed current `main` as an ancestor (`main...HEAD = 0 1`) with no task-side merge commits, and made no workspace changes or validation runs.
- On 2026-08-30, commit `798f628b` added the Alerts exact-copy regression for CRLF, bare CR, and UTF-8 bodies while retaining Markdown hydration, HTMX detail loading, pagination, project scoping, and empty-body behavior. `make templ` was rerun to a zero-update result, the server build and affected package tests passed, and `go vet ./...` plus full `go test ./... -count=1 -timeout 120s` passed. The later integration repair merged current `main` commit `d0cc0dd3` into the task branch as `eaf86cca`, regenerated the combined layout output, and re-ran deterministic generation, server build, focused layout/component/Alerts Chromium tests, vet, and the full suite successfully; only expected macOS linker warnings appeared. A subsequent fresh strict read-only audit of `eaf86cca` found no material bugs, regressions, or missing requirements and made no workspace changes or validation runs.
- Task-detail Lifecycle viewport coverage includes `TestTaskDetailContent_LifecycleTabFillsRemainingHeight` for the rendered flex/min-height chain and `TestTaskDetailLifecycleFillsRemainingHeightInChrome` for production geometry: the card reaches the task-detail bottom, the activity list owns vertical overflow, and the page root does not gain avoidable vertical overflow. The browser fixture also models DaisyUI's direct `.card-body > p` flex-growth behavior and asserts the first event begins immediately below the non-growing description, guarding the regression fixed in commit `7e5589c2`.

Performance contracts:
- File-backed production SQLite uses the measured dedicated `1W + 1R` layout: one writer pool connection and one query-only reader connection; `:memory:` fixtures remain single-connection. Representative workloads must include real task/history reads, claims, output flushes, WAL pressure, busy metrics, and positive stream flushes with zero `SQLITE_BUSY`/`SQLITE_BUSY_SNAPSHOT`.
- File-backed regressions use multiple physical `*sql.Conn` handles and real repository entry points to verify per-connection pragmas, WAL behavior, atomic writes, caller-context-bounded lock waits, timeout restoration, terminal stream protection, claims, Automation leases, thread inputs, attachment retirement/publication, incremental vacuum, serialized startup, and startup-failure cleanup.
- Timing phases must be long enough for the approximately 500 ms output ticker. Short zero-flush runs are not topology evidence. Every pooled write/write-with-`RETURNING` entry point needs deadline, cancellation-only, early-cancellation, restoration, and failure-cleanup cases.
- Preserve indexes for high-cardinality deletion and ownership lookups. Projection/performance claims need direct and end-to-end evidence at representative small and large sizes, including manual/background paths, latency, allocations, query/wait counts, response size, and contention; a narrow microbenchmark is not enough.
- Email receipt-first benchmarks cover mixed receipt states and message sizes, full-body bytes, MIME work, fetch/command/receipt/`STORE` counts, allocations, elapsed time, grouped acknowledgement, and retry recovery.

Implemented projection/freshness patterns:
- Templates, Pulse, worktree cleanup, Schedule calendar, Personality cards, Automation transition indexes, stale external-PR discovery, GitHub Dev Inbox, Chat model selection, Agent model pickers, Skills, Alerts, task-detail metrics, Reflection recent executions, channel target lookup, and task-turn skill catalogs use compact projections where summaries suffice.
- Vision routing uses semantic SQL projection, credential eligibility, tier/default selection, selected-row hydration, and environment fallback. Already-capable/no-image paths remain query-free. Runtime `list_models`, `get_model`, and `view_settings` omit secrets; `view_usage_analytics` is local-only and compact.
- Worker model-capacity paths use compact model rows; project-capacity counts use category/status/project indexes; API Chat polling uses compact execution status and batched non-Chat lookup. Chat queued recovery uses sparse indexed keyset paging.
- Runtime `view_task_thread` uses payload-free count plus bounded chronological execution pages. Positive pages use indexed reads; omitted/zero limits load bounded chunks until the existing 80 KiB transcript budget. Query-plan tests retain `idx_executions_task_started_at` without a temp sort.
- Multipart uploads use endpoint-aware caps and raw boundary handling for split headers, invalid boundary-looking prefixes, delimiter whitespace, and valid multi-file totals. Transcript bubbles use static raw-content markup and one shared hydrator.
- Makefile generation uses shared freshness checks for build, desktop, and run; malformed state, source changes/additions/removals, missing outputs, and generator-version changes regenerate conservatively. Swagger freshness covers annotated API files, non-test model/viewmodel files, non-test `internal/update` files, and execution-repository schema.

Open performance/correctness gaps:
- Projection/indexing gaps remain in Backlog, Insights, Architect, Automation work-item/history/portfolio, swarms, Chat context, autonomous triggers, Skill Analytics, plugin state, agent-owned skills, scoped files, task board, execution analytics, PR feedback, code review, Automation Live, Email receipt, Models refresh, Channels settings, and Automation history (`#269`, `#294`, `#300`, `#314`, `#321`, `#327`, `#330`, `#345`, `#350`, `#355`, `#358`, `#457`, `#490`, `#504`, `#529`, `#541`, `#546`, `#555`, `#565`, `#572`, `#594`, `#623`, `#668`, `#946`). Scheduler active-task admission issue `#926` is implemented: `SchedulerService.checkActiveTasks` now uses `TaskRepo.ListActivePendingAdmissions`, a compact metadata-only projection, while authoritative worker execution retains full-task hydration. The 20/200/1,000-task small/large benchmark matrix and regressions preserve eligibility, ordering, swarm routing, empty results, payload exclusion, and bounded one-query admission. On the 200-task large fixture, the full projection measured about `57.2 ms/op` and `20.1 MB/op` versus `0.9 ms/op` and `144 KB/op` for the compact path, roughly 98.7% lower elapsed time and 99.3% fewer allocated bytes. SQLite still used `idx_tasks_status` plus `USE TEMP B-TREE FOR ORDER BY`; no order-covering index benefit was claimed. Worker admission hydration `#42`, due-schedule fan-out `#70`, polling logs `#63`, and Pulse rendering `#277` remain distinct scopes.
- Schedule calendar `#867` concerns redundant SQL sorting after Go rebucketing; acceptance should compare query plans and production-shaped latency separately from projection work.
- Air live reload `#903` invokes `templ generate` before every Go-only build; any fix must still regenerate for `.templ` changes while avoiding unconditional generation on Go edits. This is distinct from closed Makefile generator issue `#848`.
- Lifecycle/task model-facing `agent_list` issue `#918` is implemented in PR `#934`: `agentInspector.ListAgents` uses a compact repository projection with identity/filter columns and SQL-derived ordered skill names, avoiding full prompt/tool/plugin/MCP/permission/model/source JSON hydration while preserving filtering and summary output. Regression coverage and the benchmark report about `1.12–1.17 ms/op`, `149 KB/op`, and `2,733 allocs/op` for the compact path versus `12.5–12.9 ms/op`, `7.02 MB/op`, and `13,981 allocs/op` for the full path on the 100-rich-Agent fixture. On 2026-08-30, the follow-up non-audit fix moved SQL statement counting in `TestAgentInspectorListAgentsUsesCompactProjection` off the direct inspector call and onto the actual `agent_list` runtime executor, so the exact runtime path is asserted to perform one compact query. Local commit `3401497b` contains the test-only correction; `go build ./cmd/server`, the focused regression, uncached `internal/repository`/`internal/service`, and uncached `go test ./internal/... -count=1 -timeout 120s` passed. Supported PR reuse then updated/reconciled PR `#934` at published head `52ed978278be4eb092d67459dd7d61e9c472ced6`. A fresh strict read-only audit remains required and has not yet issued a clean verdict.
- Scoped-file model-facing `grep_search` issue `#936` is open: task execution and direct-agent execution expose this path, whose current no-match behavior reads/splits each entire matching file before returning an empty result. A disposable representative probe measured a 16 MiB no-match search at about `37.4–38.4 ms` and `40.01–40.02 MB` allocated per call. The issue proposes streaming/bounded processing with explicit latency/allocation criteria and is distinct from `#668`, which covers `read_file` preview materialization; the audit left the worktree clean and `go build ./cmd/server` passed.
- Schedule primary-Agent selector issue `#940` is implemented in commit `73477384` and published as PR `#950`: `/schedule`, week-navigation HTMX refreshes, and schedule-origin task-creation refreshes use a SQL-filtered compact `id`/`name`/`model` projection, while full Agent hydration remains for Task Detail and configuration consumers. Regression coverage proves query shape, global/project availability, disabled/non-selectable/generated-archived/`archived_at` filtering, ordering, empty catalogs, model labels, initial and HTMX rendering, create refresh, and paired full `GetByID` loading. On the 1,000-rich-Agent fixture, compact option loading measured about `1.44 ms/op`, `331 KB/op`, and `8,031` allocations versus full hydration at about `58.5 ms/op`, `52.4 MB/op`, and `131,087` allocations; compact Schedule content rendering measured about `1.64 ms/op` and `470 KB/op`. A fresh strictly read-only audit of local head `73477384` found no material bugs, regressions, missing requirements, or scope violations and inspected the generated template output alongside the source. PR `#950`'s live head was `e9a9f9f9` rather than the local SHA, but it had the same `main` parent and all seven changed-file blobs matched exactly, making the published tree equivalent; its checks were still in progress at audit time. The audit ran no build, test, benchmark, formatter, generator, or other mutating command; prior implementation validation reported the server build and uncached full suite passing.
- X mention project-resolution issue `#946` is open: the live background poller fetches mention pages and, for an author without a cached project selection, scans all projects and performs one indexed authorization lookup per project. A disposable SQLite probe mirroring the current SQL measured up to `1,002` statements and about `898 µs` per unresolved-user resolution at 1,000 projects; because the fallback selection is not persisted, repeated mentions can repeat the cost. Acceptance should compare the same fixture before and after a user-keyed repository lookup, including statement count, latency, and fallback-ordering behavior while preserving explicit project selection.
Validation conventions:
- Prefer `make test`, `make test-cover`, or `go test ./... -count=1 -timeout 120s`; raw short full-suite runs can time out under load. Use readiness polling instead of fixed sleeps and `testing.Short()` for timing-sensitive tests where appropriate.
- `OPENVIBELY_SKIP_BROWSER_PERF=1 go test ./... -count=1 -timeout 240s -coverpkg=./... -coverprofile=coverage.txt` is the primary Actions coverage/compile gate and includes `cmd/server`; do not add redundant default builds without a distinct purpose. Codecov acceptance uses hosted `line_coverage` for the exact commit.
- Preserve generated-file cleanliness checks, templ coverage exclusions, packaged-update native/E2E matrices, Linux cache identity/action pinning, untrusted cache handling, setup timing artifacts, and Windows rollback helper cleanup.
- Distinguish touched-scope regressions from environment failures such as desktop/PATH setup, macOS Wails linker warnings, SQLite locks, date-sensitive schedules, plugin network timeouts, and Xcode-license Git exit `69`. On macOS, `TMPDIR=/private/tmp` is the established fallback.
