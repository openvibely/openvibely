---
name: testing_coverage_and_performance
type: project
created: 2026-06-07
updated: 2026-08-10
source: update_memory
source_id: bc683d35728f3c6cf834e99bb84fb839:ee7e5bff69c75e1f
confidence: high
title: Testing Coverage and Performance
---

OpenVibely has a large Go test suite. Durable quality work should emphasize broad behavioral coverage and production-shaped performance evidence rather than accumulating narrow happy-path tests or task-by-task benchmark notes.

Coverage direction:
- `internal/service` and `internal/handler` are the broadest coverage seams, especially error, pagination, webhook, retry, scheduling, channel, and orchestration paths.
- `internal/service/llm_service.go` needs controlled LLM caller mocks to avoid flaky tests.
- Generated templ `*_templ.go` files are excluded from coverage summaries while template tests still run.
- Prefer broad behavioral coverage over granular tests around already-covered code. Avoid blanket `t.Parallel()` around shared database setup.
- Shared mutable handler databases are unsuitable because tests mutate global/default/list state. `NewTestDB` uses an immutable serialized SQLite template to create fresh isolated in-memory fixtures while preserving UTC location, foreign keys, busy timeout, `MaxOpenConns(1)`, migrated schema, seed data, cleanup, and default-agent behavior.
- Direct runtime-tool/action tests should exercise project ownership, authorization, pagination, mode restrictions, malformed JSON, and provider-tool-incapable paths.

Performance contracts:
- SQLite uses one open connection, so unindexed scans, query fan-out, and long transactions can stall unrelated database-backed requests. Performance work must measure contention as well as isolated query latency.
- High-cardinality deletion paths must retain indexed foreign-key and cleanup-ownership lookups, especially alerts, lifecycle parent references, thread inputs, and attachment sessions. Preserve query-plan assertions for indexed `SEARCH` operations and production-shaped deletion/concurrency fixtures.
- Attachment cleanup tests must cover Clear Chat, queued/immediate publication rollback, direct Chat/task-thread failures, shared-reference retention, durable session retirement, and both upload-first and retirement-first publication-fence ordering.
- List, dashboard, and discovery surfaces should use bounded projections rather than materializing full prompts, bodies, secrets, metadata, or configuration blobs when only compact summaries are rendered.
- `list_tasks` discovery uses a private compact projection. Preserve count/page shape, relevance ordering, filters, project isolation, chat exclusion, pagination, nullable summaries, UTC timestamp serialization, and production-sized query/response/latency evidence.
- Models-page initial rendering uses compact card projections, fetches one authorized full record for Edit, and guards stale edit-detail responses. Preserve production-sized handler benchmarks and lazy secret/edit hydration.
- `ExecutionStreamHub` steady-state publication uses lazily rebuilt immutable subscriber snapshots. Preserve zero-allocation steady-state publishing and concurrent Publish/Unsubscribe/Close coverage.
- Performance changes must preserve ordering, authorization, cancellation, retries, lifecycle behavior, persistence, and projection semantics. Use representative fixtures, query plans, SQL/process/I/O counts, allocations, response sizes, and contention measurements rather than only microbenchmarks.
- Durable cost centers include handler/service tests, streaming parser and chunk reassembly, repeated database/context setup, and DB/git/worktree-heavy services.

Known bounded-projection history and current gaps:
- Templates dashboard projection was fixed by selecting bounded card columns and excluding full `default_prompt` except from `GetByID`; benchmark evidence showed large allocation and transfer reductions. Preserve the `templateCardColumns`/`scanTemplateCards` pattern.
- Open gap `#426`: Pattern Library dashboard/search/category list workflows load full `template_text` for compact cards that only render title, description, variables, category, usage, and dates. Temporary benchmark evidence at 200 patterns with 32KiB templates showed the current dashboard list path pulling about 7.2MB hidden template text per operation at roughly 8.3-8.6ms/op and 7.57MB/op, versus about 0.96-1.01ms/op and 237KB/op for a compact projection. Acceptance should add compact pattern-card projections plus lazy full-template hydration while preserving page-load aggregation, HTMX refresh/search/category/popular/recent paths, ordering, project/library semantics, and full edit/detail behavior.
- Pulse dashboard UpcomingRepo running/pending/scheduled task queries were fixed by projecting `SUBSTR(t.prompt, 1, 200)` for UI previews, preserving task fields, schedule fields, agent-name resolution, ordering, filters, and scheduled-task joins.
- Open gap `#269`: `BacklogRepo.GetBacklogTasksForAnalysis` and `GetRecentCompletedTasks` load full prompts even though only short previews are used.
- Worktree cleanup projection was fixed for `#288`: `TaskRepo.ListWithWorktrees` now selects only IDs, project ID, status, worktree path/branch, merge target branch, and merge status for `WorktreeService.CleanupMergedWorktrees`; tests cover populated and empty merge-target cleanup decisions plus non-terminal descendant branch preservation, with benchmark evidence on 300 tasks carrying 32KiB prompt/config blobs.
- Schedule calendar projection was fixed for `#303`: `TaskRepo.ListWithSchedulesByProject()` now uses a bounded calendar projection that excludes `prompt`, `chain_config`, and `swarm_config` while preserving task title/id, schedule metadata, disabled/repeat state, occurrence placement inputs, and automation schedule name overrides. Regression coverage asserts the unbounded fields are not populated, and repository benchmarks compare the old full-row shape against 20x512B and 300x32KiB fixtures.
- Open gap `#294`: Insights workflows materialize complete task prompts while retaining only 100-150 character previews.
- Open gap `#300`: Architect Task Plan queries `ListTasksBySession`/`ListTasksByPhase` select unbounded prompts even though the rendered view uses title, phase, priority, and hours.
- Open gap `#327`: Chat context building through `buildChatContext`/`ListByProjectWithCategorySorts` loads full task rows, including large `prompt`, `chain_config`, and `swarm_config` blobs, for every chat message even though only a bounded preview is rendered. Benchmark evidence at 300 tasks with 32KiB payloads showed about 17x latency and 41x memory overhead compared with a bounded projection.
- Open gap `#330`: `AutonomousTriggerService.gatherTaskContext` calls `TaskRepo.ListByProject` three times for active/backlog/completed task summaries but only reads title/category/status; full rows include `prompt`, `chain_config`, and `swarm_config`. Benchmark evidence at 300 tasks with 32KiB prompt and 4KiB chain config showed about 91x latency and 124x memory overhead compared with a bounded projection.
- Open gap `#350`: task board refreshes through `ListBoardByProjectWithCategorySorts` still select and parse full `chain_config` and `swarm_config` blobs for every card even though the rendered board only needs compact chain-enabled and swarm role/status metadata. Temporary repository benchmark evidence at 500 task cards carrying 32KiB chain configs and 32KiB swarm configs showed the current projection at about 80-83ms/op and 77.7MB/op versus a compact projection at about 13.8ms/op and 2.8MB/op with identical rendered response size.
- Open gap `#364`: Agents page list/render embeds full editable agent bodies in every card, including system prompts, tool/plugin/MCP config, skills, permissions, and search text, despite list/card workflows needing compact metadata and a bounded prompt preview. Temporary production-shaped benchmark evidence at 100 agents showed the current HTMX render at about 11.31ms/op, 11.48MB response size, and 58.8MB allocations versus a representative compact-card shape at about 1.29ms/op, 105KB response size, and 0.63MB allocations; acceptance should introduce compact card projections plus lazy edit-detail hydration while preserving current edit-modal behavior, authorization/project scoping, search semantics, and lifecycle/Automation capability data.
- Open/publication-blocked gap `#416`: Runtime `list_agents` for web Chat and channel actions should use a compact summary projection instead of `AgentRepo.List`, avoiding full prompt/tool config/plugins/permission defaults/model defaults/source-ref hydration when only name, description, non-inherit model display, skill count, and MCP server count are rendered. A 2026-08-10 implementation attempt reported passing `go build ./cmd/server && go test ./internal/... -count=1 -timeout 60s` and benchmarked the 100-large-agent compact projection at about 0.351ms/op, 37KB/op, and 727 allocs/op. Later 2026-08-10 audit-only inspections found no material code-level issue in the compact implementation, full-detail preservation, archived exclusion/order compatibility, or coverage shape. PR `#435` was opened for review with `Closes #416`, but its live file list was polluted with unrelated changes outside issue scope. A 2026-08-10 cleanup turn verified a clean issue-only local candidate at `dab63f298993617baa2645c7384cf524a03c8143` against live `main` (`b7aa062f910c6456eddb60b3fe704ad589dd33f2`) with only six `#416` files changed and passing `git diff --check && go build ./cmd/server && go test ./internal/... -count=1 -timeout 120s`; guarded branch replacement against live PR head `b0f86374d936c54960838626e2f0cd212bf46144` failed with `GitHub mutation is not authorized by the caller's Automation graph because its originating graph is no longer current`. Do not treat `#416` as complete or merged until PR `#435` is cleaned/re-published through an authorized path and a fresh audit-only review passes. Acceptance should preserve web/channel output compatibility, empty/nil/unavailable behavior, archived exclusion, name-ASC ordering, project scoping, and full hydration for edit/detail paths.
- Open/publication-blocked gap `#422`: Channels Settings webhook cards should use compact card projections plus lazy edit-detail hydration instead of loading/rendering full edit-only webhook fields for every endpoint. A 2026-08-10 local implementation attempt added `WebhookRepo.ListCardsByProject`, project-scoped webhook detail JSON, lazy edit hydration, and an order-supporting webhook card index, with focused benchmarks showing the 200-webhook card list around 1.26ms/op and 280KB/op and initial Settings HTML around 506µs/op and 569KB. Required validation passed (`make templ && go build ./cmd/server && go test ./internal/... -count=1 -timeout 60s`, plus `go test ./web/... -count=1 -timeout 120s`), but the separate audit found a material lazy edit-modal regression: the modal opens with blank edit-only fields and an enabled Save button before detail fetch completes, so a slow/failed fetch can overwrite existing `system_instructions`, `title_template`, and `prompt_template` with blanks. A 2026-08-10 fix turn added edit-mode detail readiness state, disables and guards `Save Webhook` until the detail JSON is applied, keeps Save disabled on detail-load failure, added a template regression for the guard, and passed `make templ`, `go test ./web/templates/pages -count=1 -timeout 60s`, `go build ./cmd/server`, and `go test ./web/... -count=1 -timeout 120s`. A later 2026-08-10 strictly read-only audit-only review against the updated base found a material publication-readiness issue: `web/templates/pages/settings.templ` and generated `settings_templ.go` still call undefined `selectedWebhookAgentIDsCSV('webhook_agent_ids')` from the global `htmx:configRequest` handler for `/channels/webhooks`, while the template only defines `selectedWebhookAgentIDs`, `serializeWebhookAgentData()`, and hidden field `webhook_agent_ids_hidden`. This `ReferenceError` can break HTMX create, edit, delete, test, and rotate-secret flows, violating acceptance to preserve existing webhook actions. No PR was opened; do not treat `#422` as complete until this client-script blocker is fixed and a fresh compliant audit-only review passes before publication.
- `#376` is implemented: Personality settings list/HTMX refreshes use compact custom-personality card projections carrying a bounded 150-character prompt preview instead of selecting full prompts for every card. Full prompts are lazy-loaded through one detail lookup when opening Edit, with stale-response guards. Regression coverage includes large-prompt fixtures, repository projection assertions, rendered-section assertions, and benchmark smoke coverage while preserving custom/preset edit, override, save/reset/delete/default, badges, project/sidebar, and HTMX refresh behavior.
- `#381` durable testing/performance note: Telegram username-only authorization lookups should use `idx_telegram_auth_username_lower_unknown_id` for username-only allowlist rows while preserving numeric-ID authorization, case-insensitive username matching, username-only backfill, and fail-closed behavior. Query-plan coverage should assert both numeric and username-only indexes are used without full scans, with large username-only fixtures. Publication metadata/reconciliation details are canonical in `integrations_and_channels.md`.
- `#386` optimized channel authorization allowlist list/refresh queries with covering `added_at ASC` list indexes for Telegram, Slack, Discord, and Email. Regression coverage should assert the production list queries avoid `USE TEMP B-TREE FOR ORDER BY`, preserve `added_at ASC` ordering and display/ownership fields, and keep inbound authorization lookup plans indexed. Publication/workflow reconciliation notes are canonical in `integrations_and_channels.md`.
- `#391` testing/performance contract: Slack and Discord inbound authorization should perform a single auth-table lookup for rejected users even when `checkAuthorization` receives a non-empty selected project ID, with statement-count regressions preserving allowlist semantics, project switching, nil-repo allow-all, empty-project checks, and fail-closed handling. The Telegram follow-up should use the same single system-wide lookup pattern; publication state is tracked in `integrations_and_channels.md`.
- Open/publication-blocked gap `#396`: Runtime alert listing should use compact alert summary projection(s) for JSON `list_alerts` and text/channel variants, omitting `body` and `metadata_json` while preserving project scoping, read filtering where allowed, stable pagination/order, JSON/text tool contracts, and full detail retrieval via `get_alert`. Temporary benchmark evidence over 500 alerts with large bodies/metadata showed the current full-row list path at about 6.0-6.4ms/op and 8.75MB/op versus an equivalent compact projection at about 0.061-0.065ms/op and 55.7KB/op. A 2026-08-10 implementation attempt reported passing `git diff --check && go build ./cmd/server && go test ./internal/... -count=1 -timeout 60s` plus focused projection tests and `BenchmarkAlertRuntimeListProjectionResponse`, and a later 2026-08-10 audit-only inspection found no material code-level issue in the compact projection, runtime/list wiring, full-detail preservation, filter/pagination/scoping behavior, or coverage shape. `github_open_pull_request` was rejected by Automation authorization, so no reviewable PR was opened and the gap should not be treated as merged.
- `#412` is implemented and published for review in PR `#436`: runtime `list_schedules` discovery adds goose migration `155_schedule_discovery_order_index.sql` with `idx_schedules_discovery_order` on `((next_run IS NULL), next_run ASC, created_at DESC, id ASC, task_id)`. `ScheduleRepo.ListSchedulesForDiscovery` uses the ordered index only for no-filter, first-page, multi-page requests with `limit <= 50`; filtered, deep-offset, sparse, and empty-project paths retain the existing project/task-indexed join strategy. Preserve project scoping, enabled/title/task filters, counts, pagination, nil-`next_run` ordering after pending rows, and runtime JSON fields. Regression coverage asserts no `USE TEMP B-TREE FOR ORDER BY` on the optimized path and documents filtered behavior; benchmark evidence over 25,000 target-project schedules and multiple projects showed the legacy select around 11.45-12.59ms/op versus ordered-index first page around 0.361-0.376ms/op with unchanged allocation count. A 2026-08-10 cleanup turn repaired PR `#436` after an audit found unrelated auth/channel/chat files in the PR diff: live PR head and `refs/pull/436/head` now point at clean head `b2e444fb4f0ae82fe828d73d688a7a23d3e09ce0`, and the live file list contains only `internal/database/migrations/155_schedule_discovery_order_index.sql`, `internal/database/migrations_test.go`, `internal/repository/schedule_repo.go`, and `internal/repository/schedule_repo_test.go`. The cleanup passed focused repository tests/benchmark, `go build ./cmd/server`, `go test ./internal/... -count=1 -timeout 120s`, `go vet ./...`, `go build ./...`, and `git diff --check`; macOS desktop linker warnings were non-fatal. A fresh 2026-08-10 strictly read-only audit-only review found no material implementation, validation, or publication issues; live PR `#436` was open/non-draft, targeted current `main`, had current source/PR head `b2e444fb4f0ae82fe828d73d688a7a23d3e09ce0`, included `Closes #412`, had only the four schedule-discovery files, and visible CI check runs for that head were successful. Do not treat `#412` as merged until live `main` contains it.
- `#334` Analytics Usage aggregate direction: optimize `llm_usage_events` without persisted localtime bucket columns/triggers. Direct SQLite expression indexes on `date(..., 'localtime')` are unsafe/non-deterministic for writes, and stored buckets would freeze timezone state at migration/insert/update time. The durable approach is a project/date-bounded indexed raw `occurred_at` scan with bucket labels aggregated in Go using current `time.Local`, preserving legacy SQLite localtime semantics after server/process timezone changes. Regression coverage should assert indexed scans without temp B-trees, timezone-change behavior, DST-sensitive buckets, ordering, provider/model breakdowns, cost/token aggregation, and empty/single bucket behavior. Publication state is canonical in `usage_analytics.md`.
- Open gap `#345`: Skills Analytics dashboard aggregate queries (`GetUsageOverTime`, `GetTopSkills`, `GetAgentUsage`, `GetSelectionFollowThrough`) use the same computed local-time bucket pattern over `skill_analytics_events`, so the existing `(project_id, created_at)` index does not cover grouping/ordering and SQLite uses temp B-trees for `GROUP BY`/`ORDER BY`. Benchmark evidence at 50k skill analytics events showed `GetUsageOverTime` at about 102ms/op; acceptance should add a covering expression/generated-bucket indexing strategy and query-plan regressions that remove temp sorts while preserving local-time bucket semantics.
- Open gap `#355`: Execution Analytics dashboard aggregate queries over execution history for success/failure rates, hourly trends, status breakdowns, and related endpoints use date/hour grouping or ordering shapes not covered by the current task/status/started-at indexes, causing SQLite temp B-trees while the Analytics page starts seven execution-history requests concurrently. Temporary migrated-DB evidence at 50k executions for one project showed about 116 ms median for success/failure rates and about 84 ms for hourly trends before browser rendering; acceptance should add covering expression/generated-bucket indexing or pre-aggregation with query-plan regressions that remove temp grouping/ordering sorts while preserving local-time bucket and project-scoping semantics.
- Open gap `#358`: GitHub PR feedback forwarding re-fetches all historical PR issue comments, reviews, and review comments for every open task PR on unchanged scheduled runs, then filters duplicates only after materializing the pages. Existing service tests confirm full pagination across all three sources and a second forwarder run still calls the provider before reporting duplicates; acceptance should reduce unchanged-run GitHub API/page count and decoded feedback volume while preserving pagination, authorization filtering, stable ordering, and duplicate safety.
- `#339` is implemented: goose migration `150_automation_transition_invocation_index.sql` adds `idx_automation_transitions_invocation` on `automation_transitions(project_id, automation_id, invocation_id, occurred_at, id)`, with query-plan regression and benchmark coverage in `internal/service/automation_history_test.go`. Benchmark evidence showed the invocation path moving from a temp sort to indexed lookup while preserving indexed work-item performance.
- `#314` work-item history performance direction: `ListAutomationWorkItems` pagination needs created-at ordered indexes for both unfiltered and status-filtered paths while preserving `created_at DESC, id DESC` cursor semantics. Regression coverage should assert indexed query plans without `USE TEMP B-TREE FOR ORDER BY` and preserve migration-number uniqueness during rebases.
- Unpublished gap from 2026-08-10: `ListAutomationsWithStaleExternalPullRequests` in `internal/repository/automation_runtime_repo.go` scans/groups all tracked Automation PR resources and builds a temp B-tree before discovering stale external PRs. Temporary migrated-SQLite benchmark evidence at 50,000 tracked PRs showed the current query around 97.6-102.1ms/op with no stale PRs, versus about 34.6-35.1µs/op for a stale-first shape using `task_pull_requests(updated_at, task_id)`; with one stale PR per Automation, the stale-first shape measured about 0.36-0.42ms/op. Acceptance should start from stale indexed `task_pull_requests`, avoid task-resource fan-out and temp grouping when no PRs are stale, and preserve Automation/project scoping, stale cutoff semantics, grouping of affected Automations, and reconciler behavior.
- `#321` swarm-child projection direction: `TaskRepo.ListSwarmChildren` and `FindSwarmChildByRole` should use bounded projections that omit `prompt` and `chain_config` while preserving orchestration/rendering metadata; full child payloads remain available through `GetByID`. Regression tests should assert projection shape, field preservation, and full-row reloads before worker/planner updates so existing `chain_config` is not lost during swarm follow-up handling.
- Hosted-Ubuntu dependency-cache benchmark workflow `#228` has strict sample/comparator requirements; do not claim acceptance until actual accepted artifacts exist.

CI and dependency setup:
- `go test ./... -coverprofile=coverage.out -coverpkg=./...` is the primary GitHub Actions coverage/compile gate. It includes `cmd/server`, so do not add a separate default `go build ./cmd/server` step without a distinct documented purpose.
- Preserve generated-file cleanliness checks and templ-generated-file coverage exclusions around the coverage gate.
- The packaged-update native CI matrix intentionally runs macOS and Windows only; aggregate Ubuntu `go test ./...` already covers `internal/update` on Linux.
- GitHub Actions caches Linux desktop APT archives plus a one-day metadata cache. Cache keys retain runner identity, architecture, and package-manifest digest; action references are pinned to full commit SHAs.
- Cache restores are `continue-on-error`; any non-success restore is untrusted and forces normal `apt-get update`/install. Valid cache hits preflight cached lists and use `apt-get install --no-download`; cold/stale/invalid/partial/failed restores recover normally.
- Production workflow retains machine-readable dependency-setup and complete-job timing artifacts, including cache restore outcomes, metadata validation, APT operations, archive metrics, runner identity, and benchmark labels.

Validation conventions:
- Prefer `make test`, `make test-cover`, or `go test ./... -count=1 -timeout 120s` for authoritative full validation. Raw 60-second full-suite runs can time out under load.
- Use readiness polling instead of fixed sleeps. Timing-sensitive tests may use `testing.Short()` guards.
- Distinguish repeatable touched-scope regressions from environmental failures such as desktop/config PATH, macOS Wails linker warnings, SQLite locks, date-sensitive schedules, and plugin marketplace network timeouts.
- `TMPDIR=/private/tmp` is the established fallback when default-temp plugin cloning hangs.
- Real-browser coverage is required when correctness depends on HTMX history/restoration, streaming offsets, transcript scrolling/hydration, drafts, pending attachments, stale-response races, live-refresh ordering, drag/drop, keyboard shortcuts, or other DOM timing. Handler HTML inspection alone is insufficient.
- Browser performance fixtures should run over localhost HTTP rather than `file://`, and harnesses should expose an explicit DOM pass/fail marker because headless Chrome may leave its parent process alive.
- External HTTP retry tests inject clock/backoff hooks so retries run without real sleeps; provider-specific tests cover classification, replay safety, and metadata preservation.
- Handler `NewTestContext` enables local repo paths by default; tests requiring disabled local paths must call `SetLocalRepoPathEnabled(false)`.
- Chat mode selector tests use `chat-select-change` custom events with `e.detail.value`, not native select `change` semantics.
- Malformed-JSON logs in service error-path tests are expected unless an assertion or package also fails.
- Concurrency tests should assert final/current observable state rather than requiring notification delivery to precede operation return.
- Composer shortcut/modifier browser coverage must exercise every state combination: Enter idle/active; Meta/Ctrl+Enter idle and active; Meta/Ctrl-click on Send/Stop with active steering and unavailable/idle steering. Platform coverage must include `navigator.userAgentData.platform === "iOS"` and assert Apple hint/Meta behavior.

Known correctness and coverage gaps:
- Open bug `#429`: Pattern Library JSON import persists imported templates without deriving `variables` from `template_text`, unlike create/update. Applying an imported template that contains `{{placeholders}}` can render no variable inputs and create a task with raw unresolved placeholder text; regression coverage should import such a pattern and then apply it, proving variables are extracted and substituted.
- Open bug `#408`: `parseArchitectJSONFromAI` rejects valid fenced or narrated JSON arrays for Architect task-plan generation because object extraction runs before array extraction and returns the object-unmarshal error immediately. Regression coverage should prove fenced/surrounded JSON arrays parse as task lists instead of failing after extracting the first object.
- `TaskGoalService.ResumeGoalStoppedByUser()` is production-used but lacks dedicated regression coverage. Existing `TestTaskGoalService_UserStopPausePreservesGoalForResume` exercises a different method (`ResumeGoal()`), so task resumption can regress silently.
- Resolved coverage gap (2026-08-08): completed-task Task Detail edit/goal no-rerun regressions now assert no worker queue item or running worker state, no lifecycle continuation rows, and no schedule-run marker, in addition to preserving completed status and checking no extra execution rows or `thread_inputs`.
