---
name: worktree_and_lineage
type: project
created: 2026-05-09
updated: 2026-09-14
source: after_complete
source_id: 832083a9cf9f1bc38c29e77c2e3c77a8:23de5ef1833e3bae
confidence: high
title: Worktree and Lineage
---

Coding tasks execute in isolated Git worktrees under `.worktrees/task_<id>` on task-scoped branches such as `task/<id_prefix>-<slug>`. Assigned work belongs in that worktree, not the main checkout, unless the user explicitly asks otherwise. Runtime workdir enforcement is authoritative; prompts only orient the model.

Worktree path and lifecycle:
- When `tasks.worktree_path` exists it is the effective repository root for relative shell/file operations. `LLMService.ExecuteTaskWithAgent` creates/synchronizes the worktree and handles post-execution merge. Startup sync is a real `git merge --no-edit <target>`; `MergeTargetBranch` wins, followed by local `main`, detected default branch, or `main` fallback. Remote-tracking branches are not fetched implicitly.
- Setup fails closed. Local commits need no remote, but an unborn repository has no tree for `git worktree add`; never dispatch a coding model in the main checkout when setup fails.
- Auto-merge has two independent, explicit, default-off settings with an optional target branch: `auto_merge` merges after successful execution, while `auto_merge_on_goal_achieved` merges only when the stored goal makes an exact transition to `achieved`. Completion-triggered merging is allowed during managed running finalization; goal-triggered merging requires a completed task. Both use the same lease-held, live-revalidated, idempotent merge path and remain safe when enabled together.
- Goal-triggered merging is durable rather than callback-only. Achievement is reconciled after terminalization and during maintenance, including when the repository mutation lease was busy. Reconciliation must verify task/goal identity, status, project, source branch, configured target, worktree cleanliness, and ancestry before recording `merged` or cleaning up.
- Follow-up lineage records `base_branch`, `base_commit_sha`, and `lineage_depth`. Follow-ups to merged, stale, conflict-aborted, or squash-accepted tasks start fresh from the current target; an active dirty/local follow-up may reuse its branch. Active Changes resolve committed, staged, unstaged, and untracked state consistently across diffs, summaries, cards, live fragments, and tabs.

Sandbox and safety:
- A confirmed incident edited the main checkout because prompt/tool orientation pointed there despite an assigned worktree. The durable fix is one `executionRoot` derived from `tasks.worktree_path`, shared by shell and file resolution for initial and follow-up runs.
- Writes outside the sandbox require explicit outside-workspace permission/bypass. Absolute paths and shell `cd` cannot be trusted to remain within cwd. Intentional project-root writers, including `.openvibely` system agents, retain explicit scope configuration; ordinary scoped roots resolve against `executionRoot`. Automation prompts must not present the main checkout as operative root. Do not add hard containment or deterministic prompt rewriting unless requested. Untracked-file diff synthesis must skip symlinks and enforce resolved-worktree containment (`#30`).

Git mutation and recovery:
- A process-wide repository mutation lease is keyed by Git's canonical common directory after absolute-path and symlink resolution. It coordinates setup/startup sync, commits, cleanup, publication/branch replacement, merge, rebase, and conflict recovery across aliases, linked worktrees, and service instances.
- Merge, rebase, resolve, abort, cleanup, and publication revalidate live task, project, repository, branch, target, terminal status, ancestry, conflict ownership, and operation-specific eligibility while holding the lease. Stale requests return controlled errors rather than mutating unexpected state.
- Automatic conflict recovery carries trigger identity across lease reacquisition. The resolver receives only exact Git-reported active-conflict paths and `read_file`/`write_file`/`edit_file`; it cannot use Git, staging, commits, absolute/traversal paths, `.git`, unlisted paths, symlinks, or paths outside the worktree. The application stages only marker-free known files and creates the commit; if eligibility changes, no resolution commit or target integration occurs, with only ownership-verified abort allowed to unwind the task-owned conflict.
- `MERGE_HEAD` and unmerged files identify recovery ownership. Squash conflicts without `MERGE_HEAD` restore only paths introduced by the squash/conflict, preserving unrelated staged work. NUL-delimited Git output preserves unusual filenames. Recoverable failures return authoritative refreshed fragments; genuine conflicts show Resolve/Abort and hide ordinary merge actions.

Commits and publication:
- A task remains non-terminal until managed diff capture/commit finishes. Auto-commit and GitHub subjects come from actual diff facts, not task title, prompt, output, provider, tool, status, or generated-file lists. Use concise capitalized imperative language and never follow untracked symlinks while collecting snippets.
- A local commit, clean worktree, matching filenames, or green local tests does not prove publication. Verify remote configuration, task tip, live PR head/base, exact tree/blob set, file list, checks, issue linkage, review state, and `task_pull_requests.published_head_sha`.
- GitHub PR branch replacement requires current Automation authorization and `--force-with-lease` semantics. If the guarded runtime is unavailable but an explicitly authorized repair is required, preserve the exact remote head in a named backup ref, use one exact lease, and persist the verified replacement SHA only after success. Validation or push failure leaves the prior publication snapshot unchanged.
- For issue `#1139` / PR `#1149`, the validated local fix commit is `948369c372c7c93574e32d015c47461ae27bd3d6` and the live source branch and `refs/pull/1149/head` point to published head `410b7e9d66723d296b405bddb349c75aa6c96d05`; both have tree `19a580bf3c3f349a0a726905631e93f0335e10c8`. The clean candidate is based on live `main=96ff8a75d9b3a32447599b233be8737904059c6b` and changes only `internal/service/email_service.go` and `internal/service/email_service_test.go`. PR #1149 is open, non-draft, unmerged, mergeable, targets `main`, and closes #1139. A fresh strict read-only audit on 2026-09-14 found no material implementation, workspace, publication, or requirement issue; hosted checks were still in progress at audit time. The audit ran no build, test, generation, formatting, Git mutation, or other mutating commands.
- For issue `#1137` / PR `#1150`, the latest separate non-audit repair rebased the task branch onto current local `main=63d639bf113c1db5cf984ab583359c218c3910ed` after the prior candidate's base advanced. It preserved the prior candidate `2ac2bf37345289f5471fdfefb7786cf18f79be71` at `refs/backup/ta[REDACTED]` and produced clean linear candidate `81010e12c927f00a6889e4ccb6b3d915e1aa06d5`, with `main` as its sole parent and exactly six intended files: `internal/database/migrations/184_active_task_admission_order_index.sql`, `internal/database/migrations_test.go`, `internal/repository/task_repo.go`, `internal/repository/task_repo_test.go`, `web/templates/components/chat_shared_test.go`, and `web/templates/pages/browser_process_unix_test.go`. The scheduler/index implementation was preserved; the browser overlap retains the newer shared browser-slot serialization together with the task's terminal-error fixture synchronization and graceful process-group teardown. `go build ./...`, `go vet ./...`, the database package, admission repository regressions including the performance gate, scheduler-service tests, affected component browser tests, and all page Chrome tests passed; the worktree is clean with no whitespace errors, conflict markers, or untracked artifacts. The prior known published PR/source head was `2e10613cd776d6f29f50234ceff135d3cd56dabc`; this repair did not publish or revalidate remote refs, and it intentionally stopped before the required separate fresh strict read-only audit. A subsequent fresh strict read-only audit found the branch stale again: task `81010e12c927f00a6889e4ccb6b3d915e1aa06d5` was based on `63d639bf113c1db5cf984ab583359c218c3910ed`, while current local `main=64c8b05b1f266099277c23242df984f1781ead6b` added the latest-message immediate-jump change in the shared template, generated output, and overlapping component test. The scheduler/index implementation and reconciled browser process cleanup were otherwise internally consistent, but the overlap required reconciliation; the audit ran no validation or mutations and did not establish a clean verdict.
- For issue `#1123` / PR `#1133`, a rebase-only repair preserved the polluted pre-rebase task tip `fedb89b4693aa84d2ded9eff6dfc83cdd0a3f587` at `refs/backup/ta[REDACTED]`, aborted a direct replay when stale historical commits conflicted in unrelated files, and rebuilt the task branch from local `main=00421060a1eb96a8193f8913600f703a733eb0b3` using only the verified four-file net patch. The task branch is now linear at `564eb8f42b696b9f2eebe7b874c7c74a274a33ca`, with `main` as its sole parent, a clean worktree, no merge commits in the task delta, and only `internal/database/migrations_test.go`, `web/templates/pages/automations.templ`, `web/templates/pages/automations_navigation_browser_test.go`, and `web/templates/pages/automations_templ.go` changed; stable patch ID and file contents match the pre-rebase task state. `go build ./...`, the focused Automation browser regression repeated five times, and the full `web/templates/pages` test package passed. The main checkout was not changed, though it retains pre-existing dirty memory/artifact state. No PR publication or merge into `main` was performed; the branch is ready for a fast-forward-only integration, and any required fresh strict audit must remain a separate read-only lifecycle turn.
- For issue `#1129` and PR `#1143`, the 2026-09-13 non-audit reconciliation preserved the newly observed polluted merge tip `35866ad318c191277f5ce09093d5605b3bcceba4` as `refs/backup/ta[REDACTED]` and restored the assigned worktree to the exact published candidate content at `5f55b52ae8075fc81a66d621606015fde7b4c52a`, tree `2ad0216abb36fd1a39dc3e0326785800cf49417d`, without changing local `main=45222801feb56be700ea3eaf7385a1fa318957e4`. The worktree is clean, the task branch, live source branch, and `refs/pull/1143/head` all point to `5f55b52a`, and the current live-main diff contains only the six issue-scoped files: `internal/models/project.go`, `internal/repository/project_repo.go`, `internal/repository/project_repo_test.go`, `internal/service/channel_current_project_projection_test.go`, `internal/service/chat_action_runtime.go`, and `internal/testutil/testdb.go`. The pre-publication local fix commit is preserved as `refs/backup/ta[REDACTED]`; earlier merge-forward backups remain preserved. The open non-draft PR targets live `main=8ff7116c94f7724bd2a35c8fb3addbaf448c532f`, retains its six-file list, issue linkage, updated performance matrix, and hosted-check limitation. The batch-based performance-evidence harness fix passed focused identity/query-plan/channel tests, uncached `internal/repository` and `internal/service` suites, `go build ./...`, and all eight median profiles under coverage. Supported publication reused PR #1143 and started exact-head workflow `34701889610`; all six packaged-update jobs passed, but Main job `103575003141` failed at `Run all tests with coverage`. Exact-tree local reproduction passes the changed packages and channel evidence test; the remaining local full-suite failures are four unrelated migration assertions expecting Goose `181` while the tree migrates to `183`, plus `TestFailedAssistantTerminalErrorOrderingAndSmartScrollInChrome` in `web/templates/components`. GitHub job-log access returned HTTP 403, so the limitation is documented in the PR body. The repair turn intentionally stopped before the required separate fresh strict read-only audit; completion remains blocked until that audit passes.
- On the 2026-09-13 fresh strict read-only audit following the #1129 repair, the assigned worktree had drifted again to clean merge `38d36fd1df59ea5d9671bf10099fa4a90a463520`, with published candidate `5f55b52ae8075fc81a66d621606015fde7b4c52` and local `main=45222801feb56be700ea3eaf7385a1fa318957e4` as parents, tree `9e92b0a42febc8d95f1be0e686371026634fbe82`. Its current-live-main diff again contained the six issue-scoped files plus five unrelated files: `internal/repository/task_repo.go`, `internal/repository/task_repo_test.go`, `internal/service/channel_chat_ingress.go`, `internal/service/channel_chat_ingress_model_projection_test.go`, and `internal/service/task_service.go`; prior backup refs did not preserve this new merge tip. The live task branch, PR #1143, and `refs/pull/1143/head` remained at `5f55b52a`, whose open non-draft PR still had exactly the six intended files, issue linkage, and matching issue-file blobs. Exact-head workflow `34701889610` still had all six packaged-update jobs passing but Main job `103575003141` failed at `Run all tests with coverage`, with no pending check. The read-only audit found no additional implementation defect and ran no validation or mutations; completion remains blocked by recurring worktree handoff drift and the unresolved exact-head hosted check, requiring a separate repair followed by another fresh audit.
- On the 2026-09-13 non-audit repair after the latest #1129 audit, the assigned worktree's clean merge-forward `38d36fd1df59ea5d9671bf10099fa4a90a463520` was preserved as `refs/backup/ta[REDACTED]` and the task branch/worktree was restored to exact published head `5f55b52ae8075fc81a66d621606015fde7b4c52` with tree `2ad0216abb36fd1a39dc3e0326785800cf49417d`; local `main=45222801feb56be700ea3eaf7385a1fa318957e4` and its unrelated changes were untouched. The task branch, live source branch, and `refs/pull/1143/head` all remain at `5f55b52a`, with a clean worktree and exactly the six issue-scoped files against live `main`. Supported PR reconciliation reused PR #1143 and refreshed its validation limitation without creating a new head or rerunning Actions. Exact-candidate build, identity/query-plan, four-channel, median-evidence, repository, and service checks passed; clean live-main comparison reproduced the four Goose `181` migration assertions while the base migrates to `183`. Exact-head Main job `103575003141` in workflow `34701889610` remains failed at `Run all tests with coverage`, while all six packaged jobs pass; no supported hosted rerun path is exposed, and completion still requires resolving that hosted evidence plus a separate fresh strict read-only audit.
- For issue `#1155` / PR `#1164`, the broadcaster optimization remains a clean focused implementation with immutable per-scope subscriber snapshots and typed wrapper regressions. The non-audit repair added only benchmark/evidence fixes: local commit `3c03c849` follows implementation commit `403f5162`, changes `internal/events/broadcaster_core_test.go`, `internal/events/broadcaster_test.go`, and `internal/handler/live_sse_transport_test.go`, and leaves the assigned worktree clean with exactly four intended paths against `main`. The public benchmark isolates publisher timing from channel draining, and the real transport benchmark reports paired publish-to-write latency for task, Chat, and file paths. Post-repair build, focused normal/race tests, targeted vet, and `git diff --check` passed. The fresh reconciliation fetched published head `f7b7eafb43570e007bff311d1daa4919e09e4720` into `FETCH_HEAD` only; its tree `388f4a2c26a87821728b375ad36cade9475e846e` exactly matches local `HEAD`, all four task-file blobs match, and its parent tree matches the implementation tree. The live source branch and `refs/pull/1164/head` point to that published SHA, and the authoritative PR read confirms PR #1164 is open with the complete benchmark/transport validation body and `Closes #1155`. Supported surfaces still do not expose check conclusions, review state, or a separately named `published_head_sha`; those fields remain unverified rather than inferred. No code or branch repair was needed in this reconciliation, and the required fresh strict read-only audit remains a separate next lifecycle step.
- For issue `#1136`, the implementation candidate `fdff075f9650329e552c236a3e2bd4853aa9f6dc` is a clean one-commit task branch from `main=96ff8a75d9b3a32447599b233be8737904059c6b`, changing only `internal/repository/lifecycle_repo.go` and `internal/repository/lifecycle_repo_test.go`. PR `#1148` is open, non-draft, targets `main`, closes #1136, and its synthesized published head `a77599d6b9d53a9a4cf2897e31135a8c230de6fc` has the exact candidate tree and changed-file blobs. The fresh strict read-only audit on 2026-09-12 found no material implementation, workspace, scope, or requirement issues; it intentionally ran no validation or mutation commands. Prior build and repository tests passed, while hosted checks were still in progress at audit time and the broad internal-suite limitation was documented in the PR body.
- For issue `#1128`, PR `#1141` is open against live `main` with published head `b080178a0ee6a4b7a3e4981c155b7e2dea50daec`, a single live-main parent, and exactly the two issue-scoped handler/test files whose published blobs match the audited local candidate. The fresh strict read-only audit found no material implementation, workspace, or requirement issues; it intentionally ran no validation, build, test, generation, formatting, or mutation commands.
- For issue `#1144` / PR `#1154`, the open PR targets `main`, closes the issue, and publishes source/PR refs at head `e60e0731ef1bb91304ae32b0f80e36f585c5b4a4`; its two published file blobs match the audited local tree. A fresh strict read-only audit on 2026-09-12 found no material implementation, regression, scope, workspace, or publication-content issue: the service adapter groups the three supported Anthropic operations while preserving low-level dispatch, and regression coverage preserves request/work-directory fields and rejects unsupported auth or unknown operations without provider calls. All six packaged-update checks passed, but the exact-head Main test suite failed at `Run all tests with coverage`, as it also did on the base commit; only a generic exit-code annotation was available because detailed job logs were inaccessible, so the hosted failure is pre-existing evidence but remains unclassified. The audit ran no builds or tests, and completion remains pending separate rerun/reconciliation.
- For issue `#1145` / PR `#1156`, the published open non-draft PR targets `main`, closes the issue, and has source/PR refs at head `125f5cc4cccccc3b60400bab2be78719f2a50f72`, based on `d6cac731796f058306f88fed0282be910b2345ba` with tree `d09cbf446f5621cfd810064c21814a89ea3ff2d2`. The candidate contains exactly `internal/handler/capacity_handler_test.go`, `internal/handler/project_capacity_api_projection_performance_test.go`, and `internal/handler/schedule_handler.go`; its corrected evidence compares response bodies byte-for-byte and wires the full-row baseline's `workerSvc.projectRepo` so `HasProjectCapacity` lookups are included. In the fresh strict read-only audit on 2026-09-14, the assigned worktree was clean at merge `f238328aeb6d63a4d244a7143cb909f1866d31c3`, with published head `125f5cc4...` and local `main=7947b0f91bd278784dd06096a58b93bee8e23310` as parents and tree `a72942e845614c29ac51be6d189493d8b0f63a87`; its current live-main diff contained the three issue files plus eight unrelated product paths: `internal/handler/handler_test.go`, `internal/handler/task_handler.go`, `internal/service/provider_adapter.go`, `internal/service/provider_adapter_test.go`, `web/templates/components/chat_composer_shortcuts_browser_test.go`, `web/templates/components/chat_shared.templ`, `web/templates/components/chat_shared_templ.go`, and `web/templates/components/chat_shared_test.go`. Existing backup refs preserve earlier merge-forward commits `b1fd7a6c94d5c7eb0c3df9e2f710a7b22de75a44` and `4d80a8770afe7fd5670ee6451c1651493fd583be`; the current `f238328a` tip is not separately preserved. The published candidate itself remained focused and inspection found no new implementation or requirement defect; live origin refs for `main`, the source branch, and `refs/pull/1156/head` still resolve to `125f5cc4...`. The exact-head workflow `34713893605` has six packaged-update jobs passing, but Main job `103607437517` fails at `Run all tests with coverage`; detailed hosted logs/check metadata remain inaccessible with HTTP 403, leaving the failure unresolved and unclassified. The temporary `/tmp/openvibely-1145-checks.json` artifact was removed before the audit and was absent; no mutating commands ran after the fresh strict-audit boundary. Completion remains blocked pending separate worktree repair and another fresh strict read-only audit.
- For the OAuth refresh and Analytics account-isolation repair, validated candidate `eede5d325879755c984701ada2c07cc9e9d3c07b` was followed by clean merge `acf292c12947fdf20ff01b2112b049635361c4e4`, with `eede5d32` as the first parent and `main` commit `cb413f8a6381b8d07778dda7a391e7ca75b30b1a` as the second. The merge added 14 unrelated Agent/lifecycle files. A fresh strict read-only audit found a non-forced shared-account cooldown bug, and repair commit `9dc42d5e` now scopes recent failure cooldown and deduplication to the originating OAuth config while allowing a healthy sibling to supply the shared card. The repair adds OpenAI/Anthropic production-shaped regression coverage for a persisted reconnect-required first config and healthy sibling, preserving credentials and reauthentication state. Prior repair validation passed `make templ`, `go build ./...`, `go vet ./...`, full `internal/service` tests, callback race coverage, focused Analytics regressions, and the uncached all-package suite with only the unchanged borderline projection timing gate excluded. The fresh strict read-only audit of exact HEAD `9dc42d5e99ba1706074eb3a68f58c4ba38e560ee` found another material persisted-history bug: if the healthy sibling already has a still-fresh success snapshot while a newer failure snapshot belongs to another config sharing the provider/account, refresh traversal marks the shared key seen and merge precedence displays the newer failure over healthy cached data. Existing coverage lacks this case. No workspace state changed and no validation commands ran during the audit; separate repair, validation, and another fresh strict read-only audit remain required.
- For the OAuth refresh and Analytics account-isolation repair, the clean exact-HEAD candidate `b47f844d238ab1179be65ceca281a584292c0d3f` was audited strictly read-only and exposed a stale-account-identity race: a rejected revision-conditional account-ID update could leave Analytics using a stale in-memory config, allowing a still-fresh stale token to reach usage fetch/snapshot persistence. Non-audit repair commit `56b48f9d` now reloads the authoritative config after rejected identity writes; reconnects continue only with current OAuth credentials, provider/auth edits become ineligible, reload failures abandon the candidate, and Anthropic profile results that lose a reconnect race are discarded before usage fetch or snapshotting. Production-shaped OpenAI/Anthropic regressions cover reconnect and model-edit races plus Anthropic profile-request interleaving. Build, vet, template no-drift generation, full service tests, callback race coverage, focused regressions, and all-package coverage validation passed; the unchanged `TestValidateRepoPathsProjectionProductionPerformance` timing gate passed three isolated runs but was skipped under broad load. The repair candidate is clean and requires another separate fresh strict read-only audit before completion.
- `task_commit_stats` is not shown as per-task evidence (`#723`); change parsing must preserve rename/copy paths, unusual filenames, deterministic order, live untracked files, and state fallback. The unusual-filename `--numstat` corruption issue (`#1086`) is resolved by NUL-delimited parsing for task collection and GitHub publication fallback.
