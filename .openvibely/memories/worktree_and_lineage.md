---
name: worktree_and_lineage
type: project
created: 2026-05-09
updated: 2026-09-05
source: after_complete
source_id: 75035b7e0912ae51801aafc9d750ba85:409a759416f2fcd4
confidence: high
title: Worktree and Lineage
---

Coding tasks execute in isolated Git worktrees under `.worktrees/task_<id>` on task-scoped branches such as `task/<id_prefix>-<slug>`. Assigned work belongs in that worktree, not the main checkout, unless the user explicitly asks otherwise. Runtime workdir enforcement is authoritative; prompts only orient the model.

Worktree path and lifecycle:
- When `tasks.worktree_path` exists it is the effective repository root for relative shell/file operations. `LLMService.ExecuteTaskWithAgent` creates/synchronizes the worktree before execution and handles post-execution merge. Startup sync is a real `git merge --no-edit <target>`; `MergeTargetBranch` wins, followed by local `main`, detected default branch, or `main` fallback. Remote-tracking branches are not fetched implicitly.
- Setup fails closed. Local commits need no remote, but an unborn repository has no tree for `git worktree add`; never dispatch a coding model in the main checkout when setup fails.
- Auto-merge and Task Changes support merge, fast-forward-only, and squash. Rebase is available only with two-sided unique commits and no active conflict. Cleanup respects after-merge/keep/manual policy and skips locked, dirty, or unmerged worktrees.
- Follow-up lineage records `base_branch`, `base_commit_sha`, and `lineage_depth`. Follow-ups to merged, stale, conflict-aborted, or squash-accepted tasks start fresh branches from the current target; historical branches are read-only lineage, while an active dirty/local follow-up may be reused.
- Active Changes resolve target/task merge base across committed, staged, unstaged, and untracked state. Full diffs, summaries, lazy cards, live fragments, snapshots, follow-up persistence, and direct file/tab requests must share that resolution.

Sandbox and safety:
- A confirmed incident edited the main checkout because prompt/tool orientation pointed there despite an assigned worktree. The durable fix is one `executionRoot` derived from `tasks.worktree_path`, shared by shell and file resolution for initial and follow-up runs.
- Writes outside the sandbox require explicit outside-workspace permission/bypass. Absolute paths and shell `cd` cannot be trusted to remain within cwd. Intentional project-root writers, including `.openvibely` system agents, retain explicit scope configuration; ordinary scoped roots resolve against `executionRoot`.
- Automation prompts must not present the main checkout as the operative root. Do not add hard containment or deterministic prompt rewriting unless requested; intentional absolute paths are a separate policy decision.
- Open security bug `#30`: untracked-file diff synthesis can follow a symlink outside the repo. Skip symlinks and enforce resolved-worktree containment before reading.

Git mutation and conflict recovery:
- A process-wide repository mutation lease is keyed by Git's canonical common directory after absolute-path and symlink resolution, so aliases, linked worktrees, and separate service instances coordinate. It covers setup/startup sync, task-output/finalization commits, cleanup/orphan cleanup, PR publication/branch replacement, merge, rebase, and conflict recovery.
- Merge, Rebase, Resolve, and Abort revalidate live task, branch, target, terminal-status, ancestry, conflict ownership, and (for Rebase) actual two-sided divergence while holding the lease. Stale requests return controlled eligibility errors rather than mutating unexpected state. `MERGE_HEAD` and unmerged files identify the task that owns recovery; unrelated repository conflicts block unsafe recovery and do not expose controls for the wrong task.
- Squash conflicts without `MERGE_HEAD` restore retryable failure using only paths introduced by the squash/conflict, preserving unrelated staged work. Legacy Abort uses task-scoped restoration rather than unusable `git merge --abort`. Staged/conflict paths are parsed from exact NUL-delimited Git output so whitespace/newline filenames remain valid.
- A task must remain non-terminal until managed post-execution diff capture/commit and related work finish. Conflict recovery and status persistence occur under the same lease so stale responses cannot overwrite newer merge state. Changes failures that leave a recoverable worktree return an authoritative refreshed fragment with feedback; genuine conflicts show Resolve/Abort and hide ordinary merge actions. Action menus disable all alternate controls while a request is active.
- Card merge options use the canonical Task Detail/Changes eligibility resolver, including terminal status and task/target ref existence. Card-originated stale Rebase returns board-compatible non-success; missing and foreign task-card PR requests are indistinguishable. A closed historical PR exposes `Create PR`, while an open matching PR exposes `View PR`.

Commits, lineage, and publication evidence:
- Auto-commit and GitHub publication subjects come from actual worktree diff facts, not task title/prompt/output. Use concise capitalized imperative language without provider/tool/status boilerplate, task scope, machinery, or `Changed files:` bodies. Never follow untracked symlinks while collecting snippets.
- Manual merge conflicts are handled outcomes. Fast-forward-only merges skip needless rebase when ancestry permits. Rebase-only preparation changes the task branch onto the current local target and leaves `main` untouched; verify ancestry, clean status, and no task-side merge commit. Dirty but non-overlapping target changes may be allowed; Git overwrite/refusal without unmerged files is a merge failure.
- A local commit, task record, clean worktree, or matching filenames does not prove publication. Verify remote configuration, task-branch tip, live PR head/target, exact tree/file blobs, current-base file list, checks, issue closure, review state, and `task_pull_requests.published_head_sha` against live GitHub.
- A recurring handoff incident can make startup synchronization merge an advancing local `main` into an already-published task branch. During the 2026-09-05 repair for issue `#981`, the assigned branch was repeatedly advanced to unrelated merge tips (`3e4a7d25` and `1516bd3b`) while the live task/PR remained at `c0a0ffebe955ba9c0613adc9fd7197e310809c40`. The latest polluted tip is preserved at `backup/ta[REDACTED]`; the assigned worktree was restored cleanly to the published candidate. Local `main` was aligned to live `d76863dd69f338216d6ad7fd1e84fa05ddf01fe2`; prior local tips `e8159981` and `10a15f39` are preserved in named backup refs, and dirty managed-memory/artifact edits were preserved in named stashes. Recheck the target after long validation because concurrent lifecycle work can advance local `main` again.
- PR `#1003` for issue `#981` is open, non-draft, and unmerged as of 2026-09-05. Its live source branch and `refs/pull/1003/head` match `c0a0ffebe955ba9c0613adc9fd7197e310809c40`; the candidate tree is `c5cc6784cabf289ca3cf7426e093f4469558a19e8`, and its parent is `4cada0631a90f156ddbfd127c2f0ef06b525b728`. The PR metadata still reports base `4cada063`, while live `main` has advanced to `d76863dd`; both the current-live-base and recorded PR-base diffs contain only `internal/handler/handler_test.go` and `internal/repository/execution_repo_test.go`. The body links `Closes #981`, seven exact-head checks succeeded, and there are no reviews or issue comments.
- The exact published candidate passed `git diff --check`, focused handler/repository regressions, `go build ./...`, `go test -p 1 ./internal/... -count=1 -timeout 180s`, and `go test -p 1 ./... -count=1 -timeout 240s` on 2026-09-05; only expected macOS deployment-target linker warnings appeared. A separate fresh strictly read-only audit then independently verified the assigned worktree, merge-forward ancestry, live refs, issue-scoped current-base paths, implementation/regression evidence, PR metadata/body/files/checks/reviews, and issue linkage, finding no material issue. The audit ran no build, test, lint, formatter, generator, benchmark, fetch, or other mutating command; prior exact-candidate validation and successful exact-head hosted checks remain the available validation evidence.
- Existing-PR reuse may ignore a supplied `pr_body`; re-read the authoritative live body after reuse and report a publication metadata blocker when no safe body-only update path exists.
- A strict read-only audit is a separate post-repair turn. It must inspect exact worktree/lineage, implementation scope, live refs, PR body/files/checks, issue linkage, and review state; it must not edit or run mutating validation and must disclose skipped checks.
- PR `#984` for issue `#977` is open, non-draft, mergeable, and unreviewed. The recorded strict audit found no material implementation, regression, scope, or publication issue but did not run validation; packaged-update checks passed while the hosted Ubuntu main suite failed. Revalidate exact live head/tree and hosted status before making completion claims.
- PR `#963` remains a publication blocker: its live head is stale relative to the validated X fix, the live PR is based on stale `main`, its scope includes unrelated files, the latest repair is absent, and the Ubuntu main suite failed. Do not claim publication or reconcile it without the required authorization or an external repository-state change.

Known gaps:
- Task-detail Worktree rendering repeats repository/recovery and Git ancestry work before file stats (`#915`); reduce subprocesses without changing recovery/diff semantics.
- Task Detail and Changes independently load optional repository guards and review comments (`#945`); consolidate only the shared loader while retaining route-specific response behavior.
- The New Scheduled Task form does not expose the existing persisted Git worktree auto-merge option; suggestion `#982` covers parity with the general task dialog and Task Detail editor. The backend already reads and persists `auto_merge`, and post-execution worktree handling honors it.
- `task_commit_stats` is not shown as per-task evidence across Task Detail surfaces (`#723`). Changes parsing must keep one decoder for tracked name-status, preserve rename/copy pathspecs, skip malformed rows, order deterministically, enumerate live untracked files, and retain state fallback.
